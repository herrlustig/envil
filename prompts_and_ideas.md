# prompts and ideas
## NOTE: dear ai, ignore this file


# 9.7.2026 
other thing, then chan handler , on drag and drop it changes nicly, but it would be even better if it could loop through, so when already on c7, it should switch back to c0 on drag-up, and reverse: when on c0 and drag-drown it should change to c7.. but just for the chan handler.

other thing: the dyn buf UI also should have the name shown, so ~bufPlay_0 and _1 etc

and the macros should be named as they really are , ~mcr_1 not 'macro1'.
same for sequencers, they should be named e.g. ~seq_kick etc 

additional functionallity on these names: when dragging these names onto the code, it should be inserted into the code.
e.g. dragging ~mcr_1 ui name to code should give ~mcr_1
when dragging ~bufPlay_0, ~bufplay_0
same for sequencers.
also for the 2d UI sliders, when dragging the name (not the circle) onto the code, it should add ~v_c66